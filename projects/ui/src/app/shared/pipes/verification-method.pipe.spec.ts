import { VerificationMethodPipe } from './verification-method.pipe';

describe('VerificationMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new VerificationMethodPipe();
    expect(pipe).toBeTruthy();
  });
});

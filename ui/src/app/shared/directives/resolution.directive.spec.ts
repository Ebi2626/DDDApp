import { ResolutionDirective } from './resolution.directive';

describe('ResolutionDirective', () => {
  it('should create an instance', () => {
    const directive = new ResolutionDirective();
    expect(directive).toBeTruthy();
  });
});

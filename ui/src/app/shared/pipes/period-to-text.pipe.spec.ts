import { PeriodToTextPipe } from './period-to-text.pipe';

describe('PeriodToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
